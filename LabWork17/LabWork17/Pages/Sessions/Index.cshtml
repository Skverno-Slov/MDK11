@page
@model LabWork17.Pages.Sessions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Сеанс</h1>

<p>
    <a asp-page="Create">Создать новый</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Session[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Session[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Session[0].Is3d)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Session[0].Hall.CinemaHallNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Session[0].Movie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Session) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Is3d)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hall.CinemaHallNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SessionId">Редактировать</a> |
                <a asp-page="./Details" asp-route-id="@item.SessionId">Детали</a> |
                <a asp-page="./Delete" asp-route-id="@item.SessionId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
