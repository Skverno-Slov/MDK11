<Window x:Class="LabWork11.View.Windows.MovieRedactorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork11.View.Windows" xmlns:viewmodels="clr-namespace:LabWork11.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MovieRedactorWindowViewModel}"
        mc:Ignorable="d"
        Title="MovieRedactorWindow" Height="450" Width="800">
    <StackPanel Background="Azure">
        <TextBlock Text="Название фильма"/>
        <TextBox x:Name="NameTextBox" Text="{Binding Name}"/>
        <TextBlock Text="Длительность фильма"/>
        <TextBox x:Name="DurationTextBox" Text="{Binding Duration}" PreviewTextInput="DurationTextBox_PreviewTextInput"/>
        <TextBlock Text="Год выпуска"/>
        <DatePickerTextBox x:Name="YearDatePickerTextBox" Text="{Binding Year}" PreviewTextInput="YearDatePickerTextBox_PreviewTextInput"/>
        <TextBlock Text="Описание"/>
        <TextBox x:Name="DescriptionTextBox"
     AcceptsReturn="True"
     Height="50"
     TextWrapping="Wrap"
     VerticalScrollBarVisibility="Auto"
     HorizontalScrollBarVisibility="Auto"
     Text="{Binding Description}"/>
        <TextBlock Text="Возростной рейтинг"/>
        <ComboBox x:Name="AgeRatingComboBox"
                  IsEditable="True"
                  Text="{Binding AgeRating}"
                  ItemsSource="{Binding AgeRatings}">
        </ComboBox>
        <TextBlock Text="Начало показа"/>
        <DatePickerTextBox x:Name="ReleaseBeginDatePickerTextBox" Text="{Binding ReleaseBegin}"/>
        <TextBlock Text="Конец показа"/>
        <DatePickerTextBox x:Name="DistributionEndDatePickerTextBox" Text="{Binding DistributionEnd}"/>
        <Button x:Name="SaveButton" Content="Сохранить" Margin="10" Width="100" Command="{Binding SaveCommand}" CommandParameter="{Binding Movie}" Click="SaveButton_Click"/>
    </StackPanel>
</Window>
